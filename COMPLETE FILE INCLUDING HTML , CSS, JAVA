<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AI Dropout Prediction & Counselling Dashboard</title>
  <style>
    /* ---------------- Existing CSS here ---------------- */
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: Arial,sans-serif; background: #f4f7f9; color: #333; }
    header { background: #4A90E2; color: #fff; padding: 2.2rem 1rem 1rem 1rem; text-align: center; }
    header h1 { font-size: 2.2rem; letter-spacing: 2px; line-height: 1.25; }
    header h4 { font-weight:normal; opacity:0.93; letter-spacing: 1.3px;}
    nav { display: flex; justify-content: center; background: #eaf4ff; padding: 0.7rem 0; box-shadow: 0 2px 6px rgba(0,0,0,0.08); position: sticky; top: 0; z-index: 100; }
    nav button { background: none; border: none; color: #4A90E2; font-size: 1.13rem; margin: 0 1.1rem; cursor: pointer; font-weight: bold; padding: 0.35rem 0.65rem; border-radius: 8px; transition: background 0.2s, transform 0.2s; position: relative; overflow: hidden; }
    nav button::after { content: ""; position: absolute; width: 100%; height: 100%; top: 0; left: -100%; background: rgba(74,144,226,0.15); transition: all 0.4s ease; border-radius: 8px; }
    nav button:hover::after, nav button.active::after { left: 0; }
    nav button:hover { transform: scale(1.06); }
    main { max-width: 1100px; margin: 2rem auto; min-height: 65vh; padding: 0 1.5rem; transition: opacity 0.5s ease; }
    .card { background: #fff; padding: 1.5rem 2rem; border-radius: 18px; box-shadow: 0 2px 8px rgba(0,0,0,0.11); margin-bottom: 2rem; transform: translateY(20px); opacity: 0; animation: slideFadeIn 0.7s forwards; }
    @keyframes slideFadeIn { to { transform: translateY(0); opacity: 1; } }
    #dashboardStats { display: flex; gap:2rem; flex-wrap: wrap; justify-content:center; margin-bottom:2.4rem; margin-top:1.8rem; }
    .statbox { background: linear-gradient(135deg,#eaf4ff 60%,#cbe3fa 100%); border: 1.5px solid #bbdafb; padding: 1.3rem 2.1rem; border-radius: 20px; min-width: 200px; text-align: center; box-shadow: 0 2px 7px rgba(60,150,220,0.06); margin: 0 0.7rem 0.9rem 0.7rem; transition: transform 0.22s, box-shadow 0.3s; position: relative; cursor: pointer; }
    .statbox:hover { transform: translateY(-5px) scale(1.05); box-shadow: 0 8px 20px rgba(74,144,226,0.2); }
    .statbox span { display: block; font-size:2.1rem; font-weight: bold; margin: 0.3rem 0; color: #4A90E2; animation: pop 0.4s ease; }
    @keyframes pop { 0% { transform: scale(0.7); opacity:0; } 70% { transform: scale(1.1); opacity:1; } 100% { transform: scale(1); } }
    .home-flex { display: flex; gap:4%; justify-content:space-between; align-items:stretch; margin:2.5rem 0 0 0; flex-wrap:wrap; }
    .column { flex-basis: 47%; background: #f7fbfe; border-radius: 15px; padding: 2.1rem 1.7rem; min-width: 300px; box-shadow: 0 1px 4px rgba(80,155,230,0.07); margin-bottom:24px; border-left:4.5px solid #4A90E2; transition: transform 0.4s ease, box-shadow 0.4s ease; }
    .column:hover { transform: translateY(-6px) scale(1.03) rotate(-1deg); box-shadow: 0 6px 20px rgba(0,0,0,0.12); }
    .column h3 { font-size: 1.25rem; color: #3671ac; margin-bottom: 0.8rem; text-align: left; letter-spacing: 0.5px; }
    .column ul { padding-left: 1.1rem; margin-top: 0.2rem; }
    .column li { margin-bottom: 0.8rem; line-height: 1.7; font-size: 1.07rem; text-align: left; list-style: disc inside; }
    .highlight { color: #2186d3; font-weight: bold; background: #e6efff; border-radius: 6px; padding: 2.5px 9px; margin-right: 3.5px; letter-spacing: 0.3px; }
    .home-desc { font-size:1.22rem; color:#444; text-align: center; margin-top:0.7rem; line-height:1.7; max-width: 650px; margin-left: auto; margin-right: auto; }
    form label { display: block; margin:0.5rem 0 0.2rem; font-weight:bold; }
    form input, form select { width: 100%; padding: 0.45rem; margin-bottom: 1rem; border: 1px solid #ccc; border-radius: 7px; transition: box-shadow 0.3s, border-color 0.3s; }
    form input:focus, form select:focus { border-color: #4A90E2; box-shadow: 0 0 8px rgba(74,144,226,0.3); outline: none; }
    form button { padding: 0.7rem 1.2rem; background: #4A90E2; color: #fff; border: none; border-radius: 7px; cursor: pointer; font-weight: bold; margin-top: 0.5rem; transition: background 0.3s, transform 0.2s; overflow: hidden; position: relative; }
    form button:hover { background: #357ABD; transform: scale(1.05); }
    #predictionResult { font-weight: bold; color: #333; margin-top: 1rem; }
    #riskBar { margin-top: 0.5rem; height: 20px; width: 100%; background: #eee; border-radius: 12px; overflow: hidden; display: none; }
    #riskProgress { height: 100%; width: 0; transition: width 0.8s ease-in-out, background 0.5s ease; }
    #suggestionsBox { margin-top: 1rem; padding: 1rem; border-radius: 12px; background: #f0f8ff; border-left: 7px solid #4A90E2; box-shadow: 0 2px 6px rgba(0,0,0,0.05); display: none; animation: fadeIn 0.6s; transform: translateY(15px); opacity:0; transition: transform 0.5s ease, opacity 0.5s ease; }
    #suggestionsBox.show { transform: translateY(0); opacity:1; }
    #suggestionsBox h3 { margin-bottom: 0.5rem; }
    #suggestionsList li { background: #eaf4ff; margin:0.3rem 0; padding:0.5rem; border-radius:6px; list-style:none; }
    #resourcesList { margin-top: 0.5rem; padding-left: 1.1rem; }
    #resourcesList li a { color: #357ABD; text-decoration:none; }
    #resourcesList li a:hover { text-decoration: underline; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(10px);} to { opacity: 1; transform: translateY(0);} }
    .hidden { display:none !important; }
    #chatbot { display: flex; flex-direction: column; height: 500px;}
    #chatbot-messages { flex: 1; overflow-y: auto; border: 1px solid #ddd; border-radius: 8px; padding: 0.8rem; margin-bottom: 0.5rem; background: #fafafa; }
    .msg { margin-bottom: 0.5rem; padding: 0.5rem 0.8rem; border-radius: 8px; max-width: 80%; opacity:0; transform: translateY(8px); animation: msgSlide 0.4s forwards; }
    .user-msg { background: #e0f0ff; align-self: flex-end; text-align: right;}
    .bot-msg  { background: #f1f1f1; align-self: flex-start;}
    @keyframes msgSlide { to { opacity:1; transform: translateY(0); } }
    #chatbot-input { display: flex; gap:0.5rem;}
    #chatbot-input input { flex:1; padding:0.5rem; border:1px solid #ccc; border-radius:6px; }
    #chatbot-input button { padding:0.5rem 1rem; background:#4A90E2; border:none; border-radius:6px; color:#fff; cursor:pointer; transition: transform 0.2s ease, background 0.3s ease; }
    #chatbot-input button:hover { transform: scale(1.05); background:#357ABD; }
    #chatbotTools {margin-top:1rem;}
    footer { background: #4A90E2; color:#fff; text-align:center; padding:1rem; font-size:1.05rem; transition: background 0.3s ease; }
    footer:hover { background: #357ABD; }
    @media(max-width:900px){ main{padding:0;} .home-flex { flex-direction:column; gap:0; } .column { min-width: unset; padding: 1.4rem 0.7rem; border-radius: 11px; } }
    @media(max-width:600px){ header h1{font-size:1.12rem;} .statbox{min-width:110px; font-size:0.95rem;} #chatbot{height:340px;} .card{padding: 1.05rem 0.4rem;} .home-desc{font-size:1.01rem;} }

    /* New History Table */
    #historyTable { margin-top: 2rem; border-collapse: collapse; width: 100%; }
    #historyTable th, #historyTable td { border: 1px solid #ccc; padding: 0.6rem 0.9rem; text-align: left; }
    #historyTable th { background: #eaf4ff; color:#4A90E2; }
    #downloadBtn { margin-top: 1rem; background: #2186d3; color: #fff; border: none; padding: 0.6rem 1rem; border-radius: 6px; cursor:pointer; }
    #downloadBtn:hover { background: #1971b2; }
  </style>
</head>
<body>
  <header>
    <h1>AI BASED DROPOUT PREDICTION & COUNSELLING DASHBOARD</h1>
    <h4>IIMT COLLEGE OF ENGINEERING, GREATER NOIDA (UP)</h4>
  </header>
  <nav>
    <button id="btnHome" class="active">Home</button>
    <button id="btnPredict">Prediction</button>
    <button id="btnCounselling">Counselling</button>
  </nav>
  <main>
    <!-- --- HOME PAGE --- -->
    <div id="pageHome" class="card">
      <h2 style="text-align:center;letter-spacing:1.6px;">Welcome to the Smart Student Success Dashboard</h2>
      <p class="home-desc">
        Empowering students and educators with intelligent, data-driven insights. 
        Predict dropout risk, access instant suggestions and resources, and chat with a caring AI counsellor — all in one modern platform.
      </p>
      <div id="dashboardStats">
        <div class="statbox">
          <span id="totalPred">0</span>
          Total Predictions
        </div>
        <div class="statbox">
          <span id="highRiskCount">0</span>
          High Risk Cases
        </div>
        <div class="statbox">
          <span id="modRiskCount">0</span>
          Moderate Risk Cases
        </div>
        <div class="statbox">
          <span id="lowRiskCount">0</span>
          Low Risk Cases
        </div>
      </div>
      <div class="home-flex">
        <div class="column">
          <h3><span class="highlight">🚦 Prediction System</span></h3>
          <ul>
            <li>Fill out a simple student profile to instantly receive an AI-powered dropout risk score.</li>
            <li>Smart suggestions and curated resources tailored to individual needs & risk level.</li>
            <li>Visual risk bar for quick, intuitive understanding of status.</li>
          </ul>
        </div>
        <div class="column">
          <h3><span class="highlight">💬 Counselling Tools</span></h3>
          <ul>
            <li>24/7 AI chatbot counsellor for support on stress, motivation, study skills, career, and more.</li>
            <li>Voice-enabled, student-friendly, and secure: advice is always private and confidential.</li>
            <li>Instantly connect to helplines, course platforms, and verified wellness resources.</li>
          </ul>
        </div>
      </div>
      <div style="text-align:center;margin-top:1.8rem;">
        <span style="color:#667; font-size:1.03rem;">
          <em>Start exploring the features from the menu above or check your risks on the <span class="highlight" style="background:#eafffa;color:#197c50;">Prediction</span> page!</em>
        </span>
      </div>
    </div>

    <!-- --- PREDICTION PAGE --- -->
    <div id="pagePredict" class="card hidden">
      <h2 style="font-weight:bold;">STUDENT DROPOUT PREDICTION FORM</h2>
      <h4 style="font-weight:bold;">***ALL ENTRIES ARE REQUIRED.</h4>
      <form id="predictionForm">
        <label for="name">Student Name</label>
        <input type="text" id="name" required placeholder="ENTER YOUR NAME">
        <label for="age">Age</label>
        <input type="number" id="age" required placeholder="ENTER YOUR AGE IN NUMBERS">
        <label for="performance">Academic Performance</label>
        <select id="performance" required>
          <option value="">Select</option>
          <option value="excellent">Excellent</option>
          <option value="good">Good</option>
          <option value="average">Average</option>
          <option value="poor">Poor</option>
        </select>
        <label for="attendance">Attendance (%)</label>
        <input type="number" id="attendance" required>
        <label for="support">Family Support</label>
        <select id="support" required>
          <option value="">Select</option>
          <option value="strong">Strong</option>
          <option value="moderate">Moderate</option>
          <option value="weak">Weak</option>
        </select>
        <label for="finance">Financial Stress</label>
        <select id="finance" required>
          <option value="">Select</option>
          <option value="none">None</option>
          <option value="moderate">Moderate</option>
          <option value="high">High</option>
        </select>
        <label for="mental">Mental Health</label>
        <select id="mental" required>
          <option value="">Select</option>
          <option value="stable">Stable</option>
          <option value="mild issues">Mild Issues</option>
          <option value="serious issues">Serious Issues</option>
        </select>
        <label for="extra">Extracurricular Participation</label>
        <select id="extra" required>
          <option value="">Select</option>
          <option value="active">Active</option>
          <option value="sometimes">Sometimes</option>
          <option value="none">None</option>
        </select>
        <label for="discipline">Disciplinary Issues</label>
        <select id="discipline" required>
          <option value="">Select</option>
          <option value="none">None</option>
          <option value="minor">Minor</option>
          <option value="serious">Serious</option>
        </select>
        <button type="submit">Predict</button>
      </form>
      <p id="predictionResult"></p>
      <div id="riskBar"><div id="riskProgress"></div></div>
      <div id="suggestionsBox">
        <h3>Personalized Suggestions</h3>
        <ul id="suggestionsList"></ul>
        <h3>Helpful Resources</h3>
        <ul id="resourcesList"></ul>
      </div>

      <!-- New Section: History & Download -->
      <h3 style="margin-top:2rem;">Prediction History</h3>
      <table id="historyTable">
        <thead>
          <tr>
            <th>Name</th>
            <th>Risk Level</th>
            <th>Result</th>
            <th>Date & Time</th>
          </tr>
        </thead>
        <tbody id="historyBody">
        </tbody>
      </table>
      <button id="downloadBtn">Download Report (CSV)</button>
    </div>

    <!-- --- COUNSELLING PAGE --- -->
    <div id="pageCounselling" class="card hidden">
      <h2>AI Counsellor Chatbot</h2>
      <div id="chatbot">
        <div id="chatbot-messages"></div>
        <div id="chatbot-input">
          <input type="text" id="chatbotText" placeholder="Ask me anything...">
          <button onclick="sendMessage()">Send</button>
          <button onclick="startVoice()">🎤 Speak</button>
        </div>
      </div>
    </div>
  </main>

  <footer>
    <p>© 2025 AI Dropout Prediction & Counselling | Built by CHANDRAKANT SINGH AND TEAM.</p>
  </footer>

  <script>
    // ---------- PAGE SWITCHING ----------
    function switchPage(target) {
      document.querySelectorAll('nav button').forEach(btn=>btn.classList.remove('active'));
      document.getElementById('btn'+target).classList.add('active');
      document.getElementById('pageHome').classList.add('hidden');
      document.getElementById('pagePredict').classList.add('hidden');
      document.getElementById('pageCounselling').classList.add('hidden');
      document.getElementById('page'+target).classList.remove('hidden');
    }
    document.getElementById('btnHome').onclick = ()=>switchPage('Home');
    document.getElementById('btnPredict').onclick = ()=>switchPage('Predict');
    document.getElementById('btnCounselling').onclick = ()=>switchPage('Counselling');

    // ---------- PREDICTION FORM ----------
    let predictions = JSON.parse(localStorage.getItem('predictions')) || [];
    const form = document.getElementById('predictionForm');
    const predictionResult = document.getElementById('predictionResult');
    const riskBar = document.getElementById('riskBar');
    const riskProgress = document.getElementById('riskProgress');
    const suggestionsBox = document.getElementById('suggestionsBox');
    const suggestionsList = document.getElementById('suggestionsList');
    const resourcesList = document.getElementById('resourcesList');
    const historyBody = document.getElementById('historyBody');

    function updateDashboardStats() {
      document.getElementById('totalPred').innerText = predictions.length;
      document.getElementById('highRiskCount').innerText = predictions.filter(p=>p.riskLevel===90).length;
      document.getElementById('modRiskCount').innerText = predictions.filter(p=>p.riskLevel===60).length;
      document.getElementById('lowRiskCount').innerText = predictions.filter(p=>p.riskLevel===20).length;
    }

    function updateHistoryTable() {
      historyBody.innerHTML = "";
      predictions.forEach(p => {
        const tr = document.createElement('tr');
        const date = new Date(p.timestamp).toLocaleString();
        tr.innerHTML = `<td>${p.name}</td><td>${p.riskLevel}%</td><td>${p.result}</td><td>${date}</td>`;
        historyBody.appendChild(tr);
      });
    }

    function savePredictions() {
      localStorage.setItem('predictions', JSON.stringify(predictions));
    }

    form.addEventListener('submit', function(e){
      e.preventDefault();
      const name = document.getElementById('name').value;
      const age = document.getElementById('age').value;
      const performance = document.getElementById('performance').value;
      const attendance = parseInt(document.getElementById('attendance').value);
      const support = document.getElementById('support').value;
      const finance = document.getElementById('finance').value;
      const mental = document.getElementById('mental').value;
      const extra = document.getElementById('extra').value;
      const discipline = document.getElementById('discipline').value;

      let risk = "✅ Low risk of dropout.";
      let riskLevel = 20;
      let riskColor = "green";
      let suggestions = [];
      let resources = [];

      if(performance==="poor"||attendance<50||support==="weak"||finance==="high"||mental==="serious issues"||extra==="none"||discipline==="serious"){
        risk = "⚠️ High risk of dropout. Immediate counselling recommended.";
        riskLevel = 90;
        riskColor = "red";
        suggestions.push("Work on weak subjects with extra coaching.");
        if(attendance<50) suggestions.push("Increase attendance above 75%.");
        if(support==="weak") suggestions.push("Seek mentor/peer support.");
        if(finance==="high") suggestions.push("Apply for scholarships or part-time jobs.");
        if(mental==="serious issues") suggestions.push("Consult a professional counsellor urgently.");
        if(extra==="none") suggestions.push("Join extracurriculars to build confidence.");
        if(discipline==="serious") suggestions.push("Resolve disciplinary issues with mentor guidance.");
        resources.push('<a href="https://findahelpline.com" target="_blank">☎️ Mental Health Helplines</a>');
        resources.push('<a href="https://internshala.com" target="_blank">💼 Internshala (Jobs & Internships)</a>');
        resources.push('<a href="https://www.coursera.org" target="_blank">📘 Coursera (Courses)</a>');
      } else if(performance==="average"||attendance<75||support==="moderate"||finance==="moderate"||mental==="mild issues"||extra==="sometimes"||discipline==="minor"){
        risk = "⚠️ Moderate risk of dropout. Counselling advised.";
        riskLevel = 60;
        riskColor = "orange";
        if(performance==="average") suggestions.push("Improve study habits with Pomodoro.");
        if(attendance<75) suggestions.push("Attend classes regularly.");
        if(support==="moderate") suggestions.push("Engage in group study.");
        if(finance==="moderate") suggestions.push("Plan finances & seek scholarships.");
        if(mental==="mild issues") suggestions.push("Practice meditation & stress relief.");
        if(extra==="sometimes") suggestions.push("Be more consistent in extracurriculars.");
        if(discipline==="minor") suggestions.push("Improve discipline with mentor feedback.");
        resources.push('<a href="https://www.khanacademy.org" target="_blank">📘 Khan Academy</a>');
        resources.push('<a href="https://www.calm.com" target="_blank">🧘 Calm (Relaxation)</a>');
        resources.push('<a href="https://www.scholarships.com" target="_blank">🎓 Scholarships.com</a>');
      }

      predictionResult.innerText = "Prediction: " + risk;
      riskBar.style.display = "block";
      riskProgress.style.width = riskLevel + "%";
      riskProgress.style.background = riskColor;

      suggestionsList.innerHTML = "";
      resourcesList.innerHTML = "";
      if(suggestions.length>0){
        suggestions.forEach(s => { let li = document.createElement("li"); li.innerText = s; suggestionsList.appendChild(li); });
        resources.forEach(r => { let li = document.createElement("li"); li.innerHTML = r; resourcesList.appendChild(li); });
        suggestionsBox.style.display = "block";
        suggestionsBox.classList.add("show");
      } else { suggestionsBox.style.display = "none"; suggestionsBox.classList.remove("show"); }

      predictions.push({riskLevel, name, result: risk, timestamp: Date.now()});
      savePredictions();
      updateDashboardStats();
      updateHistoryTable();
    });

    // ---------- DOWNLOAD REPORT ----------
    document.getElementById('downloadBtn').addEventListener('click', function(){
      if(predictions.length===0) { alert("No data to download!"); return; }
      let csv = "Name,Risk Level,Result,Date & Time\n";
      predictions.forEach(p => {
        const date = new Date(p.timestamp).toLocaleString();
        csv += `"${p.name}",${p.riskLevel},"${p.result}","${date}"\n`;
      });
      const blob = new Blob([csv], {type: "text/csv"});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = "Student_Prediction_Report.csv";
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
    });

    // ---------- INITIALIZE ----------
    updateDashboardStats();
    updateHistoryTable();

    // ---------- CHATBOT (existing code) ----------
    const chatbotMessages = document.getElementById('chatbot-messages');
    let chatHistory = [];
    function appendMessage(sender, message, className) {
      const msgDiv = document.createElement('div');
      msgDiv.className = "msg " + className;
      msgDiv.innerHTML = `<strong>${sender}:</strong> ${message}`;
      chatbotMessages.appendChild(msgDiv);
      chatbotMessages.scrollTop = chatbotMessages.scrollHeight;
    }

    function getBotResponse(userText) {
      const text = userText.toLowerCase();
      let response = "";
      chatHistory.push(text);
      if (chatHistory.length > 5) chatHistory.shift();
      if (text.includes("study") || text.includes("tips") || text.includes("learning")) response = "📘 Break topics into chunks, use active recall, and revise regularly. Try using Pomodoro sessions for focused study!";
      else if (text.includes("exam") || text.includes("test") || text.includes("quiz")) response = "✏️ Stay calm, revise key topics, and get enough sleep before exams. Don't forget to practice past papers!";
      else if (text.includes("attendance") || text.includes("absent")) response = "📅 Try to keep attendance above 75%. Missing classes may make topics harder to catch up on!";
      else if (text.includes("stress") || text.includes("anxiety") || text.includes("pressure")) response = "💆 Take breaks, meditate, do deep breathing, or go for a short walk. Talking to a counselor or friend can help too.";
      else if (text.includes("motivate") || text.includes("inspire") || text.includes("goal")) response = "🌟 Remember your goals! Stay consistent and positive. Celebrate small wins along the way!";
      else if (text.includes("time") || text.includes("schedule") || text.includes("plan")) response = "⏰ Make a weekly schedule, prioritize tasks, and use reminders to manage your time effectively!";
      else if (text.includes("focus") || text.includes("concentration") || text.includes("productivity")) response = "🎯 Minimize distractions, try the Pomodoro technique, and set clear goals for each study session.";
      else if (text.includes("sleep") || text.includes("food") || text.includes("exercise")) response = "🍎 Stay healthy with proper sleep, balanced meals, and regular exercise. Your mind performs best when your body is well cared for!";
      else if (text.includes("friends") || text.includes("social") || text.includes("campus")) response = "🤝 Make time for friends and social activities; a balanced student life helps reduce stress.";
      else if (text.includes("career") || text.includes("future") || text.includes("job")) response = "💼 Explore internships, learn new skills, and ask mentors for guidance. Planning early helps build a strong future!";
      else if (text.includes("hi") || text.includes("hello") || text.includes("helo")) response = "💼 hey !!!!! how may i help you...... please ask me qustions using keyword like stress, motivate, fun etc. ";
      else response = "🤖 Sorry, I can help with keywords like study, stress, motivation, focus, time management, health, career, and exams.";
      return response;
    }

    function sendMessage() {
      const input = document.getElementById('chatbotText');
      const text = input.value.trim();
      if (text) {
        appendMessage("You", text, "user-msg");
        input.value = "";
        setTimeout(() => {
          const botReply = getBotResponse(text);
          appendMessage("CounsellorBot", botReply, "bot-msg");
          speak(botReply);
        }, 700);
      }
    }

    function startVoice() {
      const recognition = new (window.SpeechRecognition || window.webkitSpeechRecognition)();
      recognition.lang = "en-US";
      recognition.start();
      recognition.onresult = function(event) {
        const voiceText = event.results[0][0].transcript;
        document.getElementById("chatbotText").value = voiceText;
        sendMessage();
      };
    }

    function speak(message) {
      if('speechSynthesis' in window){
        const utter = new SpeechSynthesisUtterance(message);
        utter.rate = 1;
        window.speechSynthesis.speak(utter);
      }
    }
  </script>
</body>
</html>
